apiVersion: v1
kind: Service
metadata:
  name: cicd-poc
  labels:
    app: cicd-poc
spec:
  selector:
    app: cicd-poc
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
